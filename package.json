{
  "name": "aces-redesign",
  "version": "0.1.0",
  "private": true,
  "description": "Mobile-first redesign of acesknust.com — CodeFest 2026 UI/UX Challenge",
  "scripts": {
    "fetch-assets": "node scripts/fetch-assets.js",
    "build": "node scripts/build.js",
    "check": "node scripts/check.js",
    "start": "npm run build && npx --yes serve site -l 8123"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
